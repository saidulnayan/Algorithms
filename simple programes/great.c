#include<stdio.h>

int main()
{
    int x=12,y=4;
    if(x>y)
    {
        printf("the greater number is: %d",x);
    }
    else
    {
        printf("the greater number is: %d",y);
    }


  return 0;

}
